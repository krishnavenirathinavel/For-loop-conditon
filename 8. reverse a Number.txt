#include <stdio.h>
int main()
{
  int n;
  scanf("%d",&n);
   int reverse = 0 , remainder;
  for(; n != 0; n = n / 10 ){
  remainder = n % 10 ;
  reverse = reverse * 10 + remainder;
  }
  printf("%d",reverse);
  return 0;
}