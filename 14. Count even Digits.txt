#include <stdio.h>
int main()
{
  int N;
  scanf("%d",&N);
  int count = 0,digit;
  for( ; N != 0; N = N/10){
    digit = N % 10;
    if( N % 2 == 0){
     count ++;
    }
    
  }
  printf("%d",count);
  return  0;
}

